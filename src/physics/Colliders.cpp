#include "Colliders.h"

namespace VE {
    // Currently, all structures in Colliders.h are plain data structures
    // No complex implementations needed yet, but keeping the cpp file for future use/compilation unit separation.
}
